name: himl
channels:
  - defaults
dependencies:
  - pip=20.1.1
  - python=3.7.3
  - pip:
      - -r build_requirements.txt
      - -r hi-ml-azure/run_requirements.txt
      - -r test_requirements.txt
