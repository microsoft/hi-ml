name: himl
channels:
  - defaults
  - pytorch
dependencies:
  - pip=20.1.1
  - python=3.7.3
  - pytorch=1.8.0
  - pip:
      - line_profiler==3.3.1
      - monai[all]==0.7.0
      - -r build_requirements.txt
      - -r test_requirements.txt
